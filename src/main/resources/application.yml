apiVersion: batch/v1
kind: Job
metadata:
  name: ${nodeName}KubeBench
spec:
  template:
    spec:
      nodeName: ${nodeName}
      containers:
      - name: my-container
        image: nginx:latest
      restartPolicy: Never